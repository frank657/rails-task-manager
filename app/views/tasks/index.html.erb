<h1>My Tasks</h1>
<% @tasks.each do |t| %>
  <li><%= t[:completed] ? '☑' : '☐'%> <%= link_to t[:title], task_path(t) %> <%= link_to 'delete', task_path(t), method: :delete, data: { confirm: 'Are you sure?' } %></li>
<% end %>

<%= link_to 'New task', new_task_path %>
